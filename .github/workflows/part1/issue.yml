name: issue-workflow
on:
  issues:
    types:
      - opened

jobs:
  issue-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world
      - name: step2
        # "|" 를 사용하면 멀티 라인으로 커멘드 작성 가능
        run: |
          echo hello world
          echo github action
